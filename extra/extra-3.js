// condition
// เงื่อนไข ถูก หรือ ผิด , true false, boolean
// < , <= , > , >= , === , == , !== , !
// || &&
let x = true;
console.log(!x);
console.log('1 === 1:', 1 === 1);
console.log('2 !== 2:', 2 !== 2);
console.log('1 >= 1:', 1 >= 1);
let check = true;

if (check) {
  // ถ้าถูกจากเงื่อนไข
  console.log('in if');
} else {
  // ถ้าผิดจากเงื่อนไข
  console.log('in else');
}